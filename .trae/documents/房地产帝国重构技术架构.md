
# 房地产帝国 - Vue3重构 技术架构设计

## 1. Architecture Design

```mermaid
graph TB
    subgraph "前端层 (Vue3)"
        A[App入口] --> B[路由层 vue-router]
        B --> C[视图组件 Views]
        C --> D[业务组件 Components]
        D --> E[UI组件库 - 自定义]
    end
    
    subgraph "状态管理层 (Pinia)"
        F[GameStore 游戏状态]
        G[UserStore 用户状态]
        H[UIModalStore 弹窗状态]
        I[ConfigStore 配置状态]
    end
    
    subgraph "业务逻辑层"
        J[GameEngine 游戏引擎]
        K[DataService 数据服务]
        L[SaveManager 存档管理]
        M[EventBus 事件总线]
    end
    
    subgraph "数据层"
        N[localStorage 持久化]
        O[InitialData 初始数据]
    end
    
    C &lt;--&gt; F &amp; G &amp; H &amp; I
    F &amp; G &amp; H &amp; I &lt;--&gt; J &amp; K &amp; L &amp; M
    K &amp; L &lt;--&gt; N &amp; O
```

## 2. Technology Description

- **Frontend**: Vue@3.4 + Typescript + Vite@5
- **CSS Framework**: TailwindCSS@3
- **State Management**: Pinia@2
- **Router**: Vue Router@4
- **Build Tool**: Vite@5
- **Package Manager**: npm / pnpm

## 3. Route Definitions

| Route | Purpose | Component |
|-------|---------|-----------|
| / | 主菜单 | MainMenu.vue |
| /game | 游戏主界面 | GameMain.vue |
| /registration/:step | 公司注册流程 | RegistrationWizard.vue |
| /investment | 投资系统 | InvestmentDashboard.vue |
| /investment/land/:id | 地块详情 | LandDetail.vue |
| /investment/auction/:id | 土地竞拍 | LandAuction.vue |
| /investment/reserves | 土地储备 | LandReserves.vue |
| /assets | 资产交易 | AssetsMarket.vue |
| /construction | 工程管理 | ConstructionDashboard.vue |
| /sales | 营销销售 | SalesDashboard.vue |
| /operations | 运营管理 | OperationsDashboard.vue |
| /capital | 资本运作 | CapitalDashboard.vue |
| /personal | 个人模块 | PersonalDashboard.vue |
| /brand | 品牌模块 | BrandDashboard.vue |
| /governance | 治理系统 | GovernanceDashboard.vue |
| /overview | 总览仪表盘 | OverviewDashboard.vue |
| /settings | 设置页面 | SettingsPage.vue |
| /saves | 存档管理 | SavesManager.vue |
| /changelog | 更新日志 | Changelog.vue |

## 4. Directory Structure

```
/
├── public/
│   └── assets/
│       ├── images/
│       └── fonts/
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── router/
│   │   └── index.ts
│   ├── stores/
│   │   ├── game.ts
│   │   ├── user.ts
│   │   ├── ui.ts
│   │   └── config.ts
│   ├── views/
│   │   ├── MainMenu.vue
│   │   ├── GameMain.vue
│   │   ├── Registration/
│   │   ├── Investment/
│   │   ├── Construction/
│   │   ├── Sales/
│   │   ├── Operations/
│   │   ├── Capital/
│   │   ├── Personal/
│   │   ├── Brand/
│   │   ├── Governance/
│   │   ├── Overview/
│   │   └── Settings/
│   ├── components/
│   │   ├── common/
│   │   ├── layout/
│   │   ├── investment/
│   │   ├── construction/
│   │   └── ...
│   ├── composables/
│   │   ├── useGameEngine.ts
│   │   ├── useSaveManager.ts
│   │   └── useNotification.ts
│   ├── services/
│   │   ├── gameEngine.ts
│   │   ├── dataService.ts
│   │   └── saveManager.ts
│   ├── types/
│   │   ├── game.ts
│   │   └── index.ts
│   ├── data/
│   │   ├── provinces.ts
│   │   ├── cities.ts
│   │   ├── initialData.ts
│   │   └── changelog.ts
│   ├── utils/
│   │   ├── formatters.ts
│   │   ├── validators.ts
│   │   └── constants.ts
│   └── styles/
│       ├── main.css
│       └── tailwind.css
├── pmc/ (原项目保持不变)
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── README.md
```

## 5. Data Model

### 5.1 Core Types

```typescript
// 游戏主状态
interface GameState {
  version: string
  isInGame: boolean
  gameTime: {
    year: number
    month: number
    day: number
  }
  company: Company | null
  player: Player | null
  landReserves: Land[]
  projects: Project[]
  aiCompetitors: AICompetitor[]
  macroEconomy: MacroEconomy
  policies: Policy[]
  achievements: Achievement[]
}

// 公司信息
interface Company {
  id: string
  name: string
  registrationProvince: string
  registrationCity: string
  establishmentDate: string
  creditCode: string
  legalRepresentative: string
  registeredAddress: string
  enterpriseType: 'limited' | 'one-person' | 'partnership'
  registeredCapital: number
  paidInCapital: number
  shareStructure: Shareholder[]
  qualificationLevel: 1 | 2 | 3 | 4
  creditRating: 'AAA' | 'AA' | 'A' | 'B' | 'C'
  cash: number
  totalAssets: number
  totalLiabilities: number
  monthlyProfit: number
  brand: Brand
  executives: Executive[]
  employees: Employee[]
  threeRedLines: ThreeRedLines
}

// 地块
interface Land {
  id: string
  province: string
  city: string
  district: string
  area: number
  floorAreaRatio: number
  buildingDensity: number
  greeningRate: number
  landUse: string
  useYears: number
  acquisitionPrice: number
  acquisitionDate: string
  status: 'pending' | 'developing' | 'completed' | 'self-held'
  currentValue: number
  tags: string[]
}

// 项目
interface Project {
  id: string
  landId: string
  name: string
  status: 'planning' | 'construction' | 'presale' | 'completed'
  constructionProgress: number
  fiveCertificates: {
    landUsePermit: boolean
    constructionPlanningPermit: boolean
    constructionPermit: boolean
    presalePermit: boolean
    completionPermit: boolean
  }
}

// 玩家
interface Player {
  nickname: string
  personalAssets: PersonalAsset[]
  relationships: Relationship[]
  abilities: Abilities
  socialStatus: SocialStatus
  risks: Risks
}

// 宏观经济
interface MacroEconomy {
  gdpGrowthRate: number
  interestRate: number
  urbanizationRate: number
  population: number
  housingPriceIndex: number
}

// 政策
interface Policy {
  id: string
  type: 'purchase-restriction' | 'loan-restriction' | 'sale-restriction' | 'three-red-lines'
  province: string | null
  startDate: string
  endDate: string | null
  content: string
}
```

## 6. Pinia Stores

### 6.1 Game Store (stores/game.ts)

```typescript
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import type { GameState, Company, Land, Project } from '@/types/game'

export const useGameStore = defineStore('game', () =&gt; {
  const gameState = ref&lt;GameState | null&gt;(null)
  const isLoading = ref(false)
  
  // Getters
  const company = computed(() =&gt; gameState.value?.company)
  const cash = computed(() =&gt; company.value?.cash ?? 0)
  const totalAssets = computed(() =&gt; company.value?.totalAssets ?? 0)
  const landReserves = computed(() =&gt; gameState.value?.landReserves ?? [])
  const projects = computed(() =&gt; gameState.value?.projects ?? [])
  
  // Actions
  function createNewGame(registrationData: any) { /* ... */ }
  function loadSave(saveData: any) { /* ... */ }
  function advanceTime() { /* ... */ }
  function updateState(updates: Partial&lt;GameState&gt;) { /* ... */ }
  
  return { 
    gameState, 
    isLoading, 
    company, 
    cash, 
    totalAssets, 
    landReserves, 
    projects,
    createNewGame, 
    loadSave, 
    advanceTime, 
    updateState 
  }
})
```

### 6.2 UI Store (stores/ui.ts)

```typescript
export const useUIStore = defineStore('ui', () =&gt; {
  const currentModal = ref&lt;string | null&gt;(null)
  const modalProps = ref&lt;any&gt;(null)
  const notifications = ref&lt;Notification[]&gt;([])
  const sidebarCollapsed = ref(false)
  
  function openModal(modalName: string, props?: any) { /* ... */ }
  function closeModal() { /* ... */ }
  function addNotification(notification: Notification) { /* ... */ }
  function removeNotification(id: string) { /* ... */ }
  
  return { 
    currentModal, modalProps, notifications, sidebarCollapsed,
    openModal, closeModal, addNotification, removeNotification
  }
})
```

## 7. Tailwind Config (tailwind.config.js)

```javascript
/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{vue,js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        game: {
          primary: '#0d1b2a',
          secondary: '#14263b',
          accent: '#FBBF24',
          'accent-end': '#D4AF37',
          card: 'rgba(20, 38, 59, 0.8)',
          success: '#10b981',
          warning: '#f59e0b',
          danger: '#ef4444',
          info: '#3b82f6'
        }
      },
      animation: {
        'hero-entrance': 'heroEntrance 0.6s ease-out forwards',
        'fade-in': 'fadeIn 0.3s ease-out',
        'slide-up': 'slideUp 0.3s ease-out'
      },
      keyframes: {
        heroEntrance: {
          '0%': { opacity: '0', transform: 'translateY(30px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' }
        }
      }
    }
  },
  plugins: []
}
```

## 8. Vite Config (vite.config.ts)

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 3000
  }
})
```

## 9. Migration Strategy

### 9.1 存档兼容性
- 保持原有 `localStorage` key 不变
- 使用适配器模式转换旧数据到新格式
- 渐进式迁移，不做破坏性改动

### 9.2 增量开发
1. 第一阶段：初始化项目，主菜单，基础框架
2. 第二阶段：迁移核心游戏状态
3. 第三阶段：实现公司注册流程
4. 第四阶段：投资系统
5. 第五阶段：其他业务模块

## 10. Performance Optimization

- 使用 Vue 3 Composition API + &lt;script setup&gt;
- 组件懒加载与代码分割
- 虚拟滚动处理大量列表
- 合理使用 Pinia 持久化
- Memo 化计算属性
